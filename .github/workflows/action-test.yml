name: hacktoberfestify

on:
  issues:
    type: opened

jobs:
  hacktoberfestify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          issue: ${{ github.event.issue.number }}
